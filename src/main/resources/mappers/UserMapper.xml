<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.kopo.final_hanaasset360.user.dao.UserDAO">
    <select id="findByUsername" parameterType="string" resultType="kr.ac.kopo.final_hanaasset360.user.vo.UserVO">
        SELECT * FROM users WHERE USERID = #{username}
    </select>
</mapper>